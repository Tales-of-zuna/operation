"use client";
const GaussJordanCalculation = ({ matrix }: { matrix: number[][] }) => {
  let x = 0;
  let y = 0;
  let pivotElement = matrix[x][y];
  let tempMatrix = matrix;
  const n = matrix.length;
  const m = matrix[0].length;

  return <div></div>;
};

export default GaussJordanCalculation;
